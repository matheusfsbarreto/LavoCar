@model IEnumerable<LavoCar.Controllers.TipoLavagem>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipos de Lavagem</h1>

<p>
    <a asp-action="Create" class=" linkLogin"><img class="miniico" style="margin-right:5px;" src="~/img/createICO.png" />Novo</a>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescTipoLav)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrecoTipoLav)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescTipoLav)
            </td>
            <td>
               R$ @Html.DisplayFor(modelItem => item.PrecoTipoLav),00
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoLavID"><img class="ico" src="~/img/edit.webp" /></a> |
                <a asp-action="Details" asp-route-id="@item.TipoLavID"><img class="ico" src="~/img/details.webp" /></a> |
                <a asp-action="Delete" asp-route-id="@item.TipoLavID"><img class="ico" src="~/img/deletar.webp" /></a>
            </td>
        </tr>
}
    </tbody>
</table>
