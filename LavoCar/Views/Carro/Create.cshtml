@model LavoCar.Controllers.Carro

@{
    ViewData["Title"] = "Create";
}

<h1>Novo Cadastro Veicular</h1>


<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Placa" class="control-label"></label>
                <input asp-for="Placa" class="form-control" />
                <span asp-validation-for="Placa" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Ano" class="control-label"></label>
                <input asp-for="Ano" class="form-control" />
                <span asp-validation-for="Ano" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MarcaModeloID" class="control-label">Marca/Modelo</label>
                <select asp-for="MarcaModeloID" class="form-control" asp-items="@(new SelectList(ViewBag.MarcaModelos, "MarcaModeloID", "DescMarcaModelo"))"></select>
            </div>
            <div class="form-group">
                <label asp-for="ClienteID" class="control-label">Nome do Cliente</label>
                <select asp-for="ClienteID" class ="form-control" asp-items="@(new SelectList(ViewBag.Clientes, "ClienteID", "NomeCliente"))"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Cadastrar" class="btn btn-primary" />
                 <a asp-action="Index"  class="btn btn-primary">Voltar</a>
            </div>
        </form>
    </div>
</div>

<div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
